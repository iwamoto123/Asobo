import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Asobo - Kids Talk App")
                .font(.largeTitle)
                .padding()
            
            Text("Hello TestFlight!")
                .font(.title2)
                .padding()
        }
    }
}

#Preview {
    ContentView()
}
